{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the settings window and dynamic notification popups",
  "windows": ["main", "notif-*"],
  "permissions": [
    "core:default",
    "core:window:default",
    "opener:default"
  ]
}
